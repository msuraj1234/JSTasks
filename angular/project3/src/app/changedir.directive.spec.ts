import { ChangedirDirective } from './changedir.directive';

describe('ChangedirDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangedirDirective();
    expect(directive).toBeTruthy();
  });
});
